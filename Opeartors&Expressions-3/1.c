#include<stdio.h>
int main()
{
 	printf("\n %d", sizeof(int));
 	
 	int a;
 	printf("\n %d", sizeof(a));
 	
 	printf("\n %d", sizeof(2));
 	
	return 0;
}
// sizeof is a compile time operator which returns size of data type or variable or constant in bytes.