#include <stdio.h>

int main()
{
    char str[10];
    scanf("%s",str);
    printf("%s",str);
    return 0;
}

// This code reads a string from the input and prints it to the output.
// The string is stored in a character array of size 10.